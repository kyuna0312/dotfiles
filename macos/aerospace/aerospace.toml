# BOX UK glow: cyan/teal gradient focus ring (JankyBorders), Box UK Contrast.
# The right-edge status bar is an Übersicht widget (macos/ubersicht/
# boxuk-bar.widget), which self-refreshes — no workspace-change hook
# needed. sketchybar is horizontal-only, so it can't do the vertical bar.
after-startup-command = [
  'exec-and-forget open -a Übersicht',
  'exec-and-forget borders active_color=gradient(top_left=0xff017c9d,bottom_right=0xff15b8ae) inactive_color=0x441b2228 width=5.0'
]

# Start AeroSpace at login
start-at-login = true

# Normalizations. See: https://nikitabobko.github.io/AeroSpace/guide#normalization
enable-normalization-flatten-containers = true
enable-normalization-opposite-orientation-for-nested-containers = true

# See: https://nikitabobko.github.io/AeroSpace/guide#layouts
# The 'accordion-padding' specifies the size of accordion padding
# You can set 0 to disable the padding feature
accordion-padding = 300

# Possible values: tiles|accordion
default-root-container-layout = 'tiles'

# Possible values: horizontal|vertical|auto
# 'auto' means: wide monitor (anything wider than high) gets horizontal orientation,
#               tall monitor (anything higher than wide) gets vertical orientation
default-root-container-orientation = 'auto'

# Mouse follows focus when focused monitor changes
# Drop it from your config, if you don't like this behavior
# See https://nikitabobko.github.io/AeroSpace/guide#on-focus-changed-callbacks
# See https://nikitabobko.github.io/AeroSpace/commands#move-mouse
# Fallback value (if you omit the key): on-focused-monitor-changed = []
on-focused-monitor-changed = ['move-mouse monitor-lazy-center']

# You can effectively turn off macOS "Hide application" (cmd-h) feature by toggling this flag
# Useful if you don't use this macOS feature, but accidentally hit cmd-h or cmd-alt-h key
# Also see: https://nikitabobko.github.io/AeroSpace/goodness#disable-hide-app
automatically-unhide-macos-hidden-apps = false

# [[on-window-detected]]
# if.app-id = 'com.apple.systempreferences'
# if.app-name-regex-substring = 'settings'
# if.window-title-regex-substring = 'substring'
# if.workspace = 'workspace-name'
# if.during-aerospace-startup = true
# check-further-callbacks = true
# run = ['layout floating', 'move-node-to-workspace S']  # The callback itself

[[on-window-detected]]
if.app-name-regex-substring = 'telegram'
run = 'layout floating'

[[on-window-detected]]
if.app-name-regex-substring = 'finder'
run = 'layout floating'

[[on-window-detected]]
if.app-name-regex-substring = 'safari'
run = 'layout floating'
 
[[on-window-detected]]
if.app-name-regex-substring = 'camera'
run = 'layout floating'

[[on-window-detected]]
if.app-name-regex-substring = 'elgato'
run = 'layout floating'

[[on-window-detected]]
if.app-name-regex-substring = 'mail'
run = 'layout floating'

[[on-window-detected]]
if.app-name-regex-substring = 'trello'
run = 'layout floating'

[[on-window-detected]]
if.app-name-regex-substring = 'quicktime'
run = 'layout floating'

[[on-window-detected]]
if.app-name-regex-substring = 'discord'
run = 'layout floating'

# Possible values: (qwerty|dvorak)
# See https://nikitabobko.github.io/AeroSpace/guide#key-mapping
[key-mapping]
preset = 'qwerty'

# Gaps between windows (inner-*) and between monitor edges (outer-*).
# Possible values:
# - Constant:     gaps.outer.top = 8
# - Per monitor:  gaps.outer.top = [{ monitor.main = 16 }, { monitor."some-pattern" = 32 }, 24]
#                 In this example, 24 is a default value when there is no match.
#                 Monitor pattern is the same as for 'workspace-to-monitor-force-assignment'.
#                 See: https://nikitabobko.github.io/AeroSpace/guide#assign-workspaces-to-monitors
# gaps_in=12, gaps_out=24 (right stays 80 for the vertical Übersicht bar)
[gaps]
inner.horizontal = 12
inner.vertical =   12
outer.left =       24
outer.bottom =     24
outer.top =        10
outer.right =      64

# 'main' binding mode declaration
# See: https://nikitabobko.github.io/AeroSpace/guide#binding-modes
# 'main' binding mode must be always presented
# Fallback value (if you omit the key): mode.main.binding = {}
[mode.main.binding]

# All possible keys:
# - Letters.        a, b, c, ..., z
# - Numbers.        0, 1, 2, ..., 9
# - Keypad numbers. keypad0, keypad1, keypad2, ..., keypad9
# - F-keys.         f1, f2, ..., f20
# - Special keys.   minus, equal, period, comma, slash, backslash, quote, semicolon, backtick,
#                   leftSquareBracket, rightSquareBracket, space, enter, esc, backspace, tab
# - Keypad special. keypadClear, keypadDecimalMark, keypadDivide, keypadEnter, keypadEqual,
#                   keypadMinus, keypadMultiply, keypadPlus
# - Arrows.         left, down, up, right

# All possible modifiers: cmd, alt, ctrl, shift

# All possible commands: https://nikitabobko.github.io/AeroSpace/commands

# See: https://nikitabobko.github.io/AeroSpace/commands#exec-and-forget
# You can uncomment the following lines to open up terminal with alt + enter shortcut (like in i3)
# alt-enter = '''exec-and-forget osascript -e '
# tell application "Terminal"
#     do script
#     activate
# end tell'
# '''

# CyberArch: SUPER+SHIFT+F fullscreen, SUPER+F float toggle, SUPER+Q close (SUPER→alt)
alt-shift-f = 'fullscreen'
alt-f = 'layout floating tiling'
alt-q = 'close'

# See: https://nikitabobko.github.io/AeroSpace/commands#layout
alt-slash = 'layout tiles horizontal vertical'
alt-comma = 'layout accordion horizontal vertical'

# See: https://nikitabobko.github.io/AeroSpace/commands#focus
# CyberArch: SUPER+arrows focus, SUPER+SHIFT+arrows move (hjkl kept)
alt-h = 'focus left'
alt-j = 'focus down'
alt-k = 'focus up'
alt-l = 'focus right'
alt-left = 'focus left'
alt-down = 'focus down'
alt-up = 'focus up'
alt-right = 'focus right'

# See: https://nikitabobko.github.io/AeroSpace/commands#move
alt-shift-h = 'move left'
alt-shift-j = 'move down'
alt-shift-k = 'move up'
alt-shift-l = 'move right'
alt-shift-left = 'move left'
alt-shift-down = 'move down'
alt-shift-up = 'move up'
alt-shift-right = 'move right'

# See: https://nikitabobko.github.io/AeroSpace/commands#resize
alt-shift-minus = 'resize smart -50'
alt-shift-equal = 'resize smart +50'

# See: https://nikitabobko.github.io/AeroSpace/commands#workspace
# CyberArch: workspaces 1-9
alt-1 = 'workspace 1'
alt-2 = 'workspace 2'
alt-3 = 'workspace 3'
alt-4 = 'workspace 4'
alt-5 = 'workspace 5'
alt-6 = 'workspace 6'
alt-7 = 'workspace 7'
alt-8 = 'workspace 8'
alt-9 = 'workspace 9'

# See: https://nikitabobko.github.io/AeroSpace/commands#move-node-to-workspace
alt-shift-1 = 'move-node-to-workspace 1 --focus-follows-window'
alt-shift-2 = 'move-node-to-workspace 2 --focus-follows-window'
alt-shift-3 = 'move-node-to-workspace 3 --focus-follows-window'
alt-shift-4 = 'move-node-to-workspace 4 --focus-follows-window'
alt-shift-5 = 'move-node-to-workspace 5 --focus-follows-window'
alt-shift-6 = 'move-node-to-workspace 6 --focus-follows-window'
alt-shift-7 = 'move-node-to-workspace 7 --focus-follows-window'
alt-shift-8 = 'move-node-to-workspace 8 --focus-follows-window'
alt-shift-9 = 'move-node-to-workspace 9 --focus-follows-window'

# CyberArch: SUPER+SHIFT+Z toggle-hud → hide/show sketchybar + collapse right gap
alt-shift-z = 'exec-and-forget ~/.config/aerospace/toggle-hud.sh'

alt-tab = 'workspace-back-and-forth'
alt-shift-tab = 'move-workspace-to-monitor --wrap-around next'

# See: https://nikitabobko.github.io/AeroSpace/commands#mode
alt-shift-semicolon = 'mode service'
alt-shift-enter = 'mode apps'

alt-t = 'exec-and-forget open -a /Applications/Telegram.app'
alt-o = 'exec-and-forget open -a /Applications/Obsidian.app'
alt-g = 'exec-and-forget open -a /Applications/WezTerm.app'
alt-s = 'exec-and-forget open -a /Applications/Safari.app'


# 'service' binding mode declaration.
# See: https://nikitabobko.github.io/AeroSpace/guide#binding-modes
[mode.service.binding]
esc = ['reload-config', 'mode main']
r = ['flatten-workspace-tree', 'mode main'] # reset layout
f = ['layout floating tiling', 'mode main'] # Toggle between floating and tiling layout
backspace = ['close-all-windows-but-current', 'mode main']
# join-with moved here (alt-shift-arrows now move windows, CyberArch style)
left = ['join-with left', 'mode main']
down = ['join-with down', 'mode main']
up = ['join-with up', 'mode main']
right = ['join-with right', 'mode main']

# 'apps' launcher mode. Enter with alt-shift-enter, then press one key to
# open an app (and drop back to main). esc leaves without launching.
# Every installed app on this Mac is reachable here, so the main-mode
# alt-<letter> binds stay free for window management.
[mode.apps.binding]
esc = 'mode main'

# --- Browsers ---
b = ['exec-and-forget open -a "Brave Browser"', 'mode main']
f = ['exec-and-forget open -a "Firefox"', 'mode main']
s = ['exec-and-forget open -a "Safari"', 'mode main']
z = ['exec-and-forget open -a "Zen"', 'mode main']

# --- Terminals ---
g = ['exec-and-forget open -a "Ghostty"', 'mode main']
w = ['exec-and-forget open -a "WezTerm"', 'mode main']
shift-w = ['exec-and-forget open -a "Warp"', 'mode main']

# --- Editors / dev ---
c = ['exec-and-forget open -a "Cursor"', 'mode main']
v = ['exec-and-forget open -a "Visual Studio Code"', 'mode main']
x = ['exec-and-forget open -a "Xcode"', 'mode main']
d = ['exec-and-forget open -a "Docker"', 'mode main']
shift-d = ['exec-and-forget open -a "DBeaver"', 'mode main']
p = ['exec-and-forget open -a "Postman"', 'mode main']
shift-f = ['exec-and-forget open -a "FileZilla"', 'mode main']

# --- AI ---
a = ['exec-and-forget open -a "Claude"', 'mode main']
shift-a = ['exec-and-forget open -a "ChatGPT"', 'mode main']

# --- Notes ---
o = ['exec-and-forget open -a "Obsidian"', 'mode main']

# --- Chat / social ---
t = ['exec-and-forget open -a "Telegram"', 'mode main']
shift-t = ['exec-and-forget open -a "TeamViewer"', 'mode main']
i = ['exec-and-forget open -a "Discord"', 'mode main']   # dIscord (d is Docker)
l = ['exec-and-forget open -a "LINE"', 'mode main']
shift-v = ['exec-and-forget open -a "Viber"', 'mode main']

# --- Media / misc ---
m = ['exec-and-forget open -a "Spotify"', 'mode main']    # Music
shift-s = ['exec-and-forget open -a "Steam"', 'mode main']
shift-b = ['exec-and-forget open -a "Blender"', 'mode main']
shift-u = ['exec-and-forget open -a "Audacity"', 'mode main']  # aUdacity

# --- Security ---
shift-p = ['exec-and-forget open -a "Burp Suite"', 'mode main']  # Pentest
h = ['exec-and-forget open -a "Hydra"', 'mode main']

# [workspace-to-monitor-force-assignment]
#     1 = '^dell$'
#     2 = '^dell$'
#     3 = '^dell$'
#     4 = '^dell$'
#     5 = 'main'
#     6 = '^elgato$'
